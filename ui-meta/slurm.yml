name: "slurm"
label: "Slurm"
description: "Slurm OpenHPC cluster."

parameters:
  - name: "cluster_num_workers"
    label: "Worker nodes"
    description: "The number of worker nodes in the cluster."
    kind: "integer"
    options:
      min: 1

  - name: "storage_name"
    label: "Home directory storage"
    description: "Name of the storage cluster to use for user home directories."
    kind: "string"
    default: ""
