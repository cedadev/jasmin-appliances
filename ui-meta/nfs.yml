name: "nfs"
label: "NFS"
description: "NFS storage server."

parameters:
  - name: "nfs_volume_size"
    label: "Volume size (GB)"
    description: "NFS storage server volume size."
    kind: "integer"
    options:
      min: 5
